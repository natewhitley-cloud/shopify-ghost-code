# Session Handoff: 2026-06-15 — Top-10 Remediation (6/7 done, 4A next)

Branch: `code-review-2026-06-12` (9 commits ahead of where the session started at `de5285b`). Not yet pushed / no PR opened.

## What Got Done
6 of 7 actionable top-10 fixes from `docs/code-review-2026-06-12.md`, each independently verified (typecheck clean + full vitest green before closing). Test suite 1308 → 1367.

| Ticket | Commit | What |
|--------|--------|------|
| 9A (GC-q4k) | `7c810a4`+`df8a0ae` | react-router 7.13.1→7.17.0 (RCE + 21 vulns cleared); fixed duplicate `@shopify/shopify-api` regression via session-storage-prisma 8→9 |
| 1A (GC-s47) | `fd067e9` | Expanded GHOST_TITLE/OG safe-var allowlists + golden tests (Dawn/Sense/Refresh/Craft/Spotlight) |
| 6A (GC-i8c) | `bcc14fd` | `app/lib/scope-check.server.ts` (ACCESS_DENIED vs transient); rewrote scan-theme test harness to actually run audits |
| 5A (GC-fp2) | `d868855` | `saveThemeFindings`+`finalizeScan` split; `ScanStatus.PARTIAL`; `Scan.skippedCategories`; differ no longer false-resolves un-audited categories |
| 2A (GC-wsn) | `c1c20c7` | GHOST_PRICE requires orphan evidence (discount-app metafield e.g. Bold `inventory.ShappifySale`) |
| 3A (GC-y92) | `3a12c47` | GHOST_TRANSLATION → LOW informational, non-accusatory copy; removed dead `installedAppNames` (`detectOrphanedTranslations`→`detectTranslationContent`) |
| 8A (GC-ruo) | `bb8d877` | Removed dead `Shop.accessToken` + `getShopByDomain` + token-encryption module; `upsertShop(domain)` only; added upsertShop tests (TST-1) |

## Deploy-time actions required (owner)
1. **Two Prisma migrations are PENDING, not applied** (authored by hand, not run against the live Railway DB): `20260614000000_add_partial_status_and_skipped_categories` and `20260615000000_drop_shop_access_token`. They apply automatically on deploy via `prisma migrate deploy` (in `docker-start`), which runs before the app serves traffic — ordering is safe. Do not run `migrate dev` against prod.
2. **Remove `TOKEN_ENCRYPTION_KEY` from Railway** — no longer used after 8A.

## In-Progress / Next: 4A (GC-eis) — the focused session this handoff is for
**Goal:** contextual App Bridge optional-scope requests so the 4 product-data audits (product tags, prices, metafields, pages, redirects) + translations actually get their scopes granted. Currently NO code requests optional scopes (`shopify.app.toml` declares them but nothing calls `shopify.scopes.request()`), so those audits never run in prod.

**Ready to build — all dependencies done:** the audit pipeline (5A) and scope-error handling (6A) are correct, detectors (1A/2A/3A) are fixed, and graceful degradation is already wired (missing scope → `skipped:true` → PARTIAL). `read_translations` STAYS (3A kept translations as informational).

**Pickup points / files:**
- `app/shopify.server.ts` — only scope ref is `scopes: process.env.SCOPES?.split(",")`; optional scopes live in `shopify.app.toml` `optional_scopes`.
- Add an App Bridge `shopify.scopes.request([...])` flow — per-category, triggered from settings (`app/routes/app.settings.tsx`) when the merchant enables an audit category.
- Track granted scopes via the `scopes_update` webhook (`app/routes/webhooks.app.scopes_update.tsx` exists).
- Surface available-vs-needs-permission state in the settings UI.

## Open Questions for 4A (need owner/UX input)
- **Scope-request UX placement** (`app/routes/app.settings.tsx` vs an onboarding step): contextual per-category toggles (higher grant rate, more UI) vs one-time onboarding bundle (simpler, all-or-nothing). Owner picked "contextual" at plan time — confirm the exact surface.
- **Verification needs a Shopify dev store** — the live grant modal can't be tested headless. This is why 4A was deferred to a session where the owner can run `shopify app dev` and click through the grant flow.

## Risks & Warnings
- **Flaky test (GC-9x2 / TST-6):** the full suite failed 3/1354 once under heavy concurrent load; unreproducible in 9+ runs. Clock-dependent quota/middleware tests. If a CI run goes red intermittently, this is the likely cause — not a real regression.
- **5 esbuild advisories remain (GC-e8a):** build-time only, not runtime-reachable; need a major vite/@react-router-dev bump. Deliberately not forced.
- **Uncommitted:** `memory/MEMORY.md` + `memory/team/retro-history.md` (this retro's edits) are unstaged, consistent with leaving `memory/` churn uncommitted. Commit if you want them tracked.
- No PR opened yet — the 9 commits live on `code-review-2026-06-12` only.

## Deferred (need owner action, not code-blocked)
- 7A (GC-25u): Managed Pricing link — needs the real app handle from Partner Dashboard.
- 10A (GC-664): Railway Postgres backups (dashboard action) + restore runbook + remove dead `better-sqlite3` dep (the dep removal needs no owner action and could be done anytime).
