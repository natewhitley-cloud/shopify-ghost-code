# Archived Learnings: implementer

- Layer boundaries: Routes → Services → Models → Prisma. Never import routes from services. (archived: 2026-03-10, reason: passive — covered by rule: .claude/rules/architecture.md)
- Route modules use loader() for data fetching, action() for mutations — no separate API route files. (archived: 2026-03-10, reason: passive — covered by rule: .claude/rules/architecture.md + .claude/rules/definition-of-done.md)
- GraphQL queries use cursor-based pagination with `first: 250` and `after` cursor. (archived: 2026-03-10, reason: passive — covered by rule: .claude/rules/shopify-graphql.md)
- Rate limiting: 50 points/second for GraphQL. Monitor `extensions.cost.throttleStatus` in responses. (archived: 2026-03-10, reason: passive — covered by rule: .claude/rules/shopify-graphql.md)
- Polaris uses `<s-*>` Web Components (CDN), NOT React imports. No `import { Card } from '@shopify/polaris'`. (archived: 2026-03-10, reason: passive — covered by rule: .claude/rules/polaris-web-components.md)
- Session tokens, not cookies — never use localStorage or document.cookie for auth. (archived: 2026-03-10, reason: passive — covered by rule: .claude/rules/shopify-graphql.md Authentication section)
- GDPR webhooks must ALL return 200 even if no data to process. Missing webhooks = automatic app rejection. (archived: 2026-03-10, reason: passive — covered by rule: .claude/rules/gdpr-and-billing.md)
- Webhook handlers must ALWAYS return 200. Use authenticate.webhook() for HMAC verification. Non-200 causes infinite Shopify retries. (archived: 2026-03-10, reason: passive — covered by rule: .claude/rules/gdpr-and-billing.md, duplicate of GDPR entry)
- App signature regex patterns must match BOTH assignment form (hjid=) and object-key form (hjid:). (archived: 2026-03-10, reason: narrow — only relevant when editing app-signatures.server.ts)
- LINK_STYLESHEET_RE needs two capture-group branches for both attribute orderings (rel-first and href-first). (archived: 2026-03-10, reason: narrow — only relevant when editing scan-engine regex)
- InngestMiddleware afterExecution is BlankHook (zero args). Capture timing via closure in onFunctionRun scope, not hook params. (archived: 2026-03-10, reason: narrow — only relevant to middleware.ts)
- Structure cross-file detection as explicit numbered passes (Pass 1: per-file, Pass 2: cross-file). (archived: 2026-03-10, reason: narrow — scan engine internals)
- Fingerprint-based diffing: use multiset (Map of counts) not Set. getPreviousScanForTheme filters to COMPLETED status. (archived: 2026-03-10, reason: narrow — scan-differ internals)
- Snippets can render other snippets in Liquid. File reference analyzer must include snippet files in the reference scan pass. (archived: 2026-03-10, reason: narrow — file-reference-analyzer internals)
- `export { Foo as Bar }` re-export pattern satisfies framework named-export contracts (React Router's ErrorBoundary). (archived: 2026-03-11, reason: narrow — one-time pattern)
- BillingInterval values: OneTime, Every30Days, Annual, Usage. trialDays is top-level on the plan, not on lineItems. (archived: 2026-03-11, reason: narrow — billing setup done)
- APP_SUBSCRIPTIONS_UPDATE webhook payload: `{ app_subscription: { name, status, admin_graphql_api_id } }`. Status ACTIVE = live; all others = revert to free tier. (archived: 2026-03-11, reason: narrow — webhook handler done)
- isTest is NOT part of the billing plan config in shopifyApp(). Pass it at call sites via `billing.require({ isTest: process.env.NODE_ENV !== "production" })`. (archived: 2026-03-11, reason: narrow — billing setup done)
- app/uninstalled fires immediately; shop/redact fires 48h later. Both paths clean up via deleteShopData(). (archived: 2026-03-11, reason: narrow — webhook handlers done)
- Three-state branch pattern (isFailed/isRunning/isCompleted derived booleans before JSX) is cleaner than inline status checks. (archived: 2026-03-11, reason: established — already used consistently)
- createScan uses callback-form $transaction for atomic TOCTOU guard (check + create in one tx). completeScanWithFindings uses array-form $transaction with deleteMany prepended for idempotency. (archived: 2026-03-31, reason: STALE — completeScanWithFindings changed to interactive transaction in session 28; entry now incorrect)
- Webhook handlers receive shop domain, not internal DB ID. Model functions for webhooks need domain-keyed lookups (e.g., updateShopPlanByDomain). (archived: 2026-03-31, reason: low — no upcoming webhook work)
- countScansForShopSince lives in scan.server.ts (model layer) — plan-gating imports it. Keep queries in models, not lib. (archived: 2026-03-31, reason: low — quota mechanism already built)
- billing.request() throws a redirect response internally — never returns a value. (archived: 2026-03-31, reason: low — billing setup done)
- fetchMainTheme is the canonical shared function in app/services/theme-fetcher.server.ts. Returns { id, name, updatedAt }. Do not inline the MAIN theme GraphQL query — import from here. Dynamic import required inside Inngest step.run() callbacks. (archived: 2026-03-31, reason: low — theme fetcher stable, no upcoming changes)
- When a dashboard shows badge counts for an in-progress scan, render "—" instead of "0" to avoid misleading merchants. (archived: 2026-03-31, reason: low — already implemented)
- Use useRef for poll counters and mount-time state captures (no re-renders). useState only for values that drive UI updates (e.g. pollingTimedOut for banner). (archived: 2026-03-31, reason: low — polling already built)
- shopify.toast.show() accepts { isError?: boolean; duration?: number } as second arg. Use isError: true for FAILED state toasts. (archived: 2026-03-31, reason: low — narrow, already known)
- Files with $ in the name (e.g. app.scans.$scanId.tsx) must be single-quoted when passed to git add — unquoted, the shell expands $var to empty string. (archived: 2026-03-31, reason: low — narrow operational)
- When updating a price constant, grep for the human-readable string form (e.g., `$59`) in UI routes — settings/pricing pages often duplicate the value as display text. (archived: 2026-03-31, reason: low — no pricing changes upcoming)
- "First scan free" is a gating-layer concept in plan-gating.server.ts, not a plan feature — hasCompletedScans() in scan model detects first-ever scan. (archived: 2026-03-31, reason: low — already built)
- Free-tier preview pattern: return `previewFinding` (single highest-severity) server-side via getHighestSeverityFinding(), keep full findings array empty. Category breakdown uses findingSummary.byType with display-name mapping in the component. (archived: 2026-03-31, reason: low — already built)
- Health score is a pure function in app/lib/health-score.ts (client-safe, no .server.ts). computeHealthScore takes severity counts + totalFiles, returns { score, label, tone }. (archived: 2026-03-31, reason: low — already built)
- Use getPlanFeatures(plan).autoRescan to gate Pro-plan features — avoids hardcoding plan name strings scattered across the codebase. (archived: 2026-03-31, reason: low — already built)
- themes/publish webhook records lastThemePublishAt on Shop model for non-Pro nudge banners. Migration: add-shop-theme-publish-timestamp. (archived: 2026-03-31, reason: low — already built)
- getScanById accepts optional `{ includeFindings?: boolean }` — use false for routes that don't need findings (e.g., free-tier scan detail). Prisma conditional include returns a union type; cast is needed at usage. (archived: 2026-03-31, reason: low — already built)
- Static data files (app-signatures.server.ts) are append-only with zero coupling risk — skip dependency tracing, just verify with tsc. (archived: 2026-03-31, reason: low — narrow)
- Fan-out worker reuse: Standard weekly scan and Professional daily scan both fan out to the same poll-check-shop worker. Plan filtering belongs in the coordinator, not the worker. (archived: 2026-03-31, reason: low — already built)
- When a detector needs to match multiline content (like JSON-LD blocks), use a multiline regex on full file content with an offset-to-line-number helper (lineNumberAtOffset), rather than forcing line-by-line iteration. (archived: 2026-03-22, reason: low — narrow, scan engine internals)
- PLAN_AMOUNTS in subscription webhook must stay in sync with billing config in shopify.server.ts. If prices change, both need updating. (archived: 2026-03-31, reason: low — billing done)
- `replacementBehavior` is a static plan config property in shopify.server.ts billing config, not a per-request param on billing.request(). Set APPLY_ON_NEXT_BILLING_CYCLE on the target plan so downgrades defer to next billing cycle. (archived: 2026-03-31, reason: low — billing done)
- Billing page needs in-app downgrade buttons, not just upgrade. Shopify Admin only offers cancel, not plan-switch. Without downgrade buttons, merchants must cancel + resubscribe (losing trial days, temporary feature gap). Use billing.request() with replacementBehavior: APPLY_ON_NEXT_BILLING_CYCLE for downgrades so merchants keep features until cycle ends. Only show downgrade buttons when on a paid tier. (archived: 2026-03-31, reason: low — already built, cross-app transfer applied)
- Polaris `<s-*>` valid prop values: s-badge tone (info/critical/auto/neutral/success/caution/warning, NOT 'attention'), s-text (no fontWeight/variant — use `<strong>`/`<code>`), s-stack gap ('base'/'loose', NOT 'tight'). s-banner uses `tone="critical"` (NOT `status="critical"`). (archived: 2026-06-15, reason: relevance low — UI, no UI work in Tier-1 sprint; retrievable before next UI dispatch)
- React Router `useActionData` returns `undefined` for non-2xx action responses. To surface errors through `actionData`, return 200 with error payload, or use `useFetcher`. (archived: 2026-06-15, reason: relevance low — route-action gotcha, sprint is worker/webhook path)
- app/lib/ for client-safe utility modules (no .server.ts), app/components/ for shared UI components. Both patterns established. (archived: 2026-06-15, reason: relevance low — structural/UI, off-sprint)
- s-button valid variants are: primary, secondary, tertiary, auto — 'plain' is not supported. (archived: 2026-06-15, reason: relevance low — UI, off-sprint)
- When integrating two services with different input shapes, document the adapter inline at the mapping site. (archived: 2026-06-15, reason: relevance low — general/aging, no specific sprint tie)
- File-local component extraction (no new file) is appropriate for route-specific UI patterns that don't need cross-route sharing. (archived: 2026-06-15, reason: relevance low — UI, off-sprint)
- Resource routes (no default export) return Response directly from loader — use Content-Type + Content-Disposition headers for file downloads. Polaris `<s-link>` doesn't support `download` attribute; rely on Content-Disposition: attachment. (archived: 2026-06-15, reason: relevance low — UI/download, off-sprint)
- Optimistic local state + fetcher.submit is the right pattern for instant UI feedback on dismiss/toggle actions in embedded Shopify apps. (archived: 2026-06-15, reason: relevance low — UI, off-sprint)
- When converting a per-line regex detector to FULL-CONTENT matching (to catch multi-line tags), use the existing `lineNumberAtOffset` helper for line attribution, and precompute skip-line Sets from a prior line pass rather than restructuring to track offset ranges — least invasive. (archived: 2026-06-15, reason: relevance low — deferred C8 detector work, retrievable 2026-09-15)
- DANGER after per-line→full-content conversion: JS `\s*`/`\s+` then matches `\n`, so a pattern like RENDER_RE matches multi-line tags AND a second `/m`-anchored line-start pattern matches the inner line — DOUBLE-counting. Dedup by OFFSET RANGE (record `[match.index, match.index+len]` claimed by the first pattern, skip overlapping hits), NOT by derived line numbers, which differ across patterns for the same token. Always add a multi-line-token regression test. (archived: 2026-06-15, reason: relevance low — deferred C8 detector work, retrievable 2026-09-15)
- To match a bare Liquid statement at a line start (e.g. `render`/`include 'x'` inside a `{% liquid %}` block) use `/^[ \t]*(?:render|include)\s+["']...["']/gim` — the `^…/m` anchor avoids matching `render` inside HTML comments/strings without needing lookbehind. (archived: 2026-06-15, reason: relevance low — deferred C8 detector work, retrievable 2026-09-15)
- Conditional-aware meta detectors: track comment lines via the shared `buildCommentSkipLines(content)` helper (extracted in GC-b34); conditional-depth (`{% if/unless/case %}` ++ / `{% endif/... %}` --, skip when depth>0) stays inline per-detector. Meta tags can't legally appear inside `{% liquid %}` blocks, so bare if/endif there is a non-issue. (archived: 2026-06-15, reason: relevance low — deferred C8 detector work, retrievable 2026-09-15)
- NOT every per-line→full-content conversion has a double-count trap. The trap exists ONLY when a detector uses a tag-form pattern AND a separate line-anchored pattern matching the same token (the snippet/render detector). SAFE: single-regex detectors (even with internal `A|B` alternation — JS matches once per position, e.g. canonical's rel/href) and detectors whose patterns target mutually-exclusive syntaxes (fetch vs `$.ajax` vs xhr `.open`). Verify with `node -e` on a multi-line input, then state "no dedup needed" explicitly rather than adding unneeded offset-range machinery. (archived: 2026-06-15, reason: relevance low — deferred C8 detector work, retrievable 2026-09-15)
- To preserve comment/conditional line-skip semantics during a full-content conversion, precompute skip-line Sets from ONE `lines()` pass (`buildCommentSkipLines` + a `LIQUID_CONDITIONAL_RE` pass) and test `lineNumberAtOffset(file.content, match.index)` against them — least-invasive, matches the old per-line `continue` behavior exactly. (archived: 2026-06-15, reason: relevance low — deferred C8 detector work, retrievable 2026-09-15)
- In a full-content regex detector, compute `lineNumberAtOffset(content, match.index)` and run the comment/conditional skip-Set checks BEFORE the appName/attribution logic (`identifyAppFromSnippetName` etc.) — you need the line number to skip, and gating attribution behind the skip avoids a wasted lookup on suppressed matches. The old per-line code computed lineNumber after the appName guard; reorder it on conversion. (archived: 2026-06-15, reason: relevance low — deferred C8 detector work, retrievable 2026-09-15)
- React Router v7 web-path error hook: `export function handleError(error, { request, context, params })` from `app/entry.server.tsx` is called for errors thrown in loaders/actions/render (mirrors the Inngest middleware path). Forward to `captureException` but guard `if (request.signal.aborted) return` to avoid Sentry spam from cancelled navigations. Also call `captureException` inside the `renderToPipeableStream` `onError` callback for render-phase errors. (archived: 2026-06-15, reason: relevance low — shipped observability GC-c09, off-sprint)
- Timeout-race pattern for liveness probes (e.g. `/health` DB check): `await Promise.race([db.$queryRaw\`SELECT 1\`, timeoutPromise])` where timeoutPromise rejects after ~2s, with a `finally` that `clearTimeout`s the timer. Return an explicit 503 Response on reject — don't throw. (archived: 2026-06-15, reason: relevance low — shipped health-check GC-c09, off-sprint)
