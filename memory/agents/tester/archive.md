# Archived Learnings: tester

- completeScanWithFindings uses array-form $transaction (db.$transaction([op1, op2])), not callback-form. Mock with `$transaction: vi.fn(async (ops) => Promise.all(ops))`. (archived: 2026-04-01, reason: relevance passive — GC-06c changed to interactive transaction, entry is now incorrect)
- canStartScan uses db directly for active-scan guard + delegates to countScansForShopSince from scan model. Both need independent mocks. (archived: 2026-04-01, reason: relevance low, freshness stale — implementation detail discoverable from code)
- analyzeFileReferences scans ALL liquid files (including snippets) for render/include tags — transitive snippet→snippet references are correctly resolved. Tests must account for this. (archived: 2026-04-01, reason: relevance low, freshness stale — scanner internals discoverable from code)
- (from implementer) fetchMainTheme in theme-fetcher.server.ts has 3 unit tests (success, null, error). Scan detail page now has 3 testable behaviors: polling timeout banner, completion toast, no-toast on initial terminal load. (archived: 2026-04-01, reason: relevance low — snapshot of past coverage, test file is source of truth)
- GraphQL responses from Shopify include `extensions.cost` — mock this in API response fixtures. (archived: 2026-04-01, reason: relevance low, freshness stale — discoverable from existing test fixtures)
- Inngest function tests need realistic event payloads with shopId, scanId, themeId. (archived: 2026-04-01, reason: relevance low, freshness stale — existing tests demonstrate the pattern)
- Tests mirror app/ structure in `tests/` directory (e.g., `tests/services/scan-engine.test.ts`). (archived: 2026-07-08, reason: relevance passive — covered by rule: .claude/rules/testing.md File Location)
- Use `npx vitest run <path>` for single-file runs without the npm script alias. (archived: 2026-07-08, reason: relevance passive — covered by rule: .claude/rules/testing.md Commands)
- Shopify's embedded admin has Cloudflare captcha — automated E2E (Playwright/Cypress) is impractical. Focus on unit + integration tests. (archived: 2026-07-08, reason: relevance passive — covered by rule: .claude/rules/testing.md Manual E2E)
- Theme file content in test fixtures should use real Liquid syntax with known ghost code patterns. (archived: 2026-07-08, reason: relevance passive — covered by rule: .claude/rules/testing.md Unit Tests)
- Prefer testing behavior (what the function returns) over implementation details (which internal method was called). (archived: 2026-07-08, reason: relevance passive — covered by rule: .claude/rules/testing.md Patterns)
- Every service function should have both happy-path and error-path tests. (archived: 2026-07-08, reason: relevance passive — covered by rule: .claude/rules/testing.md Patterns "happy path, empty input, error case")
- REPO-WIDE pre-commit gotcha: the pre-commit hook runs `eslint --max-warnings 0` with `import/order` — local relative imports must be path-sorted or the commit is REJECTED. REFINEMENT (GC-kde): the eslint config sorts SIBLING (`./x`) BEFORE PARENT (`../x`); prefer `npx eslint --fix <files>` (autofixer is authoritative). PROCESS FIX: subagents must run `npx eslint --max-warnings 0 <changed files>` before reporting done — tsc + prettier do NOT catch import/order or prefer-const. (archived: 2026-07-08, reason: relevance passive — promotion completed: covered by rule: .claude/rules/imports.md, corrected S20 to autofix-first)
- billing.server.ts getPlanFeatures() is a pure function — ideal smoke test, no mocks needed. (archived: 2026-07-08, reason: relevance low, freshness stale — trivial, discoverable from code)
- When mocking `inngest/client` for functions using `inngest.createFunction()` at module load, include `createFunction: vi.fn((_config, _trigger, handler) => ({ fn: handler }))`. Without it, the source module throws at import time. (archived: 2026-07-08, reason: relevance low, freshness stale — no upcoming Inngest work; retrieve before next Inngest test dispatch)
- Inngest function handlers accessible via scanTheme.fn — call directly as fn({ event, step }) to bypass SDK runtime. (archived: 2026-07-08, reason: relevance low, freshness stale — no upcoming Inngest work; retrieve with the createFunction-mock entry)
- formatDate uses toLocaleDateString('en-US') — ISO string inputs parsed as UTC midnight can shift by one day in negative-UTC-offset envs. Use Date object constructors with local-time args, or noon UTC times + regex matching. (archived: 2026-07-08, reason: relevance low, freshness stale — no upcoming date/UI work)
- For exhaustive enum coverage, pair individual named tests per value (readability + pinpoint failure) with a loop over all values that asserts set membership (forward-compatibility guard). (archived: 2026-07-08, reason: relevance low, freshness stale — general pattern, no upcoming enum work)
- When testing weighted scoring formulas, derive expected outputs by hand before writing expect() calls — comment the derivation inline. Prefer inputs that produce integer normalized deductions to avoid rounding surprises. (archived: 2026-07-08, reason: relevance low, freshness stale — health-score work done)
- (from reviewer) Any detector that uses a regex-pattern SPLIT (e.g. one pattern for `{% render %}` tag-form + another for bare `render` line-form) is a latent DOUBLE-COUNT risk. Always include a fixture containing BOTH forms (incl. a multi-line tag) in the same file and assert on the TOTAL finding count — not just "≥1 finding exists". (archived: 2026-07-08, reason: relevance low, freshness aging — deferred C8 detector work; retrieve with implementer's C8 archive cluster)
- (from reviewer) Golden-fixture tests for detectors should use VERBATIM upstream theme source (e.g. Shopify Dawn `snippets/meta-tags.liquid`) asserting zero false findings — a scanner's credibility rests on never flagging stock themes. This is the standard pattern for detector regression tests. (archived: 2026-07-08, reason: relevance low, freshness aging — deferred C8 detector work; retrieve with implementer's C8 archive cluster)
- (from debugger) To test a module-load boot guard (`if (NODE_ENV==='production' && !key) throw`), use `vi.stubEnv('NODE_ENV','production')` + `vi.resetModules()` + `await expect(import('../path')).rejects.toThrow(...)`; assert the dev case does NOT throw. Vitest defaults `NODE_ENV` to "test", so production-gated guards never fire in the suite — but always `vi.unstubAllEnvs()` in afterEach so stubs don't bleed into sibling tests. (archived: 2026-07-08, reason: relevance low, freshness aging — boot-guard work shipped, no upcoming env-gated code)
- To test an `InngestMiddleware` instance, invoke `await middleware.init()` → `.onFunctionRun({ fn, ctx })` to get the per-run hooks (`afterExecution` / `transformOutput`). Confirmed via `node_modules/inngest/components/InngestMiddleware.d.ts` (`init` is a readonly property on the instance). `onFunctionRun`'s TS return is a union that won't narrow at the call site — wrap the call in a helper with an explicit cast return type or tsc fails even when tests pass at runtime. Fakes: `fn = { name, id: () => "kebab-id" }`, `ctx = { event: { name }, runId }`, transformOutput ctx = `{ result: { error } }`. (archived: 2026-07-08, reason: relevance low, freshness aging — middleware tests shipped, no upcoming middleware work)
- Month/week-boundary flakes: a relative comparison like `result.getUTCMonth() >= before.getUTCMonth()` silently fails at the Dec→Jan rollover when the test reads the real clock. Pin it: `vi.useFakeTimers()` + `vi.setSystemTime(new Date('2026-06-15T12:00:00.000Z'))` (noon-UTC to dodge the offset day-shift), assert EXACT month/year values not relative comparisons. NOTE: a mocked `getWeekStartUTC` is already deterministic — only tests that hit the REAL clock flake. (archived: 2026-07-08, reason: relevance low, freshness aging — flake hardening done, no upcoming date work)
- `vitest.config.ts` coverage.include is now `app/**/*.{ts,tsx}` (was `.server.ts`-only) with `app/routes/**` no longer excluded — non-.server libs and routes now report coverage. No thresholds configured, so broadening include can't fail CI. (archived: 2026-07-08, reason: relevance low, freshness aging — config-state snapshot, discoverable from vitest.config.ts)
- Testing TTL/time-based code deterministically: give the unit under test an INJECTABLE clock — `createTtlCache(ttlMs, now = () => Date.now())` — and in tests pass `() => nowMs` with a mutable `let nowMs`, advancing it by assignment. `vi.useFakeTimers()` does NOT work if the module captured a bare `Date.now` reference at import (the default must be a thunk to stay mockable). For code you can't refactor to inject, `vi.spyOn(Date, "now")` works since the cache reads `Date.now()` at call time — restore in afterEach. (archived: 2026-07-08, reason: relevance low, freshness aging — TTL cache work shipped GC-kde)
